# Copy to .env and adjust as needed for your deployment.
# These are read by docker-compose only — local dev is unaffected.

# Host port the controller service listens on (default: 8000)
CONTROLLER_PORT=8000

# Host port the node service listens on (default: 8001)
NODE_PORT=8001
